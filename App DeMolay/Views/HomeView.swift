import SwiftUI

struct HomeView: View {
    var body: some View {
        NavigationStack {
            VStack(alignment: .leading, spacing: Spacing.xl) {
                
            }
            .navigationTitle("Olá, Kowa")
            .navigationSubtitle("Capela Grande N.º 656")
        }
    }
}

#Preview {
    HomeView()
}
